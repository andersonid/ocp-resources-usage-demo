apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-dashboard
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: https://github.com/andersonid/ocp-resources-usage-demo.git
    targetRevision: main
    path: gitops/demo-dashboard
  destination:
    server: https://kubernetes.default.svc
    namespace: demo-dashboard
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
